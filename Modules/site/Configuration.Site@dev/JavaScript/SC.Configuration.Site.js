define('SC.Configuration.Site', [
    'SC.Configuration',
    'underscore'
], function SCConfigurationSite(
    SCConfiguration,
    _
) {
    'use strict';
    /**
     * DISCOURAGED: please use SC Configuration record and JSON files
     * In case you really need, you can still do it via code here
     * To use it, add your global config here. It will be merged in the specific app config, not here
     */
    return {
    };
});
